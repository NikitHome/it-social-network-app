import React from 'react';
import style from "./Settings.module.css"

const Settings = (props) => {
    return (
        <div className={style.developmentSettingsPage}>
            The page is in development :)
        </div>
    );
}

export default Settings;